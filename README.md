# okresky

## Vývoj

Projekt používá:

- Node.js `24.15.0` podle `.node-version`
- pnpm `11.7.0` podle `packageManager` v `package.json`

Lokální spuštění:

```sh
fnm use
pnpm install
pnpm dev:portless
```

Web poběží na <https://main.okresky.localhost:1355> nebo jiné branch URL.
